// lib
var React = require('react');

/**
 * React component for rendering a list of in-flight data file uploads.
 */
var DataFileUploadStatus = module.exports = React.createClass({
    displayName: 'DataFileUploadStatus',

    render: function() {
        return (
            <div></div>
        );
    }
});
